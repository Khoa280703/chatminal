// Session/runtime-entry spawn helpers are included from `desktop_termwindow_spawn.rs`
// at the `termwindow` module level so they can access private state without
// leaking compatibility vocabulary into this directory.
