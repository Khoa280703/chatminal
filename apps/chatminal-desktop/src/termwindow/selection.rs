// Selection helpers are included from `desktop_termwindow_selection.rs` at the
// `termwindow` module level to keep private-state access while shrinking the
// compatibility surface in this directory.
